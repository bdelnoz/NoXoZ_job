# services/ingestion.py

async def parse_and_store_file(file):
    """
    Fonction placeholder pour ingestion d'un fichier.
    À remplacer par le vrai parsing + stockage vectoriel.
    """
    filename = file.filename
    # Ici tu peux ajouter : parsing PDF/DOCX/MD + stockage Chroma/SQLite
    return f"Fichier {filename} reçu et prêt à être traité"
